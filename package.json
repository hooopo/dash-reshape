{
  "name": "reshape-github-dashboard",
  "version": "0.0.1",
  "scripts": {
    "build": "evidence build",
    "dev": "evidence dev -o",
    "test": "evidence build"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "type": "module",
  "dependencies": {
    "@evidence-dev/evidence": "^11.0.0",
    "@octokit/graphql": "^5.0.5",
    "dotenv": "^16.0.3"
  }
}
